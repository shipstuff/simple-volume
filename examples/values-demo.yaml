storagePools:
  - name: default
    path: /tmp/simple-volume-demo
    defaultReplicaCount: 2
    allowNonEmpty: false

sync:
  token: "demo-token"

replication:
  webdav:
    enabled: true
    port: 8081
  fullResyncSchedule: "0 4 * * *"

networkPolicy:
  enabled: false
